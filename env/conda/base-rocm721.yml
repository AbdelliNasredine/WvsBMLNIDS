name: rocm721-py312
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      # torch==2.9.1+rocm7.2.1
      # torchvision (matching rocm7.2.1 cp312 wheel)
      # torchaudio  (matching rocm7.2.1 cp312 wheel)
      # rocm_sdk_core / rocm_sdk_devel / rocm_sdk_libraries_custom
      - numpy==1.26.4